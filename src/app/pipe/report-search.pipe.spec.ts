/* tslint:disable:no-unused-variable */
import {ReportSearchPipe} from "./report-search.pipe";

describe('ReportSearchPipe', () => {
  it('create an instance', () => {
    let pipe = new ReportSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
